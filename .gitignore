node_modules/
dist/
.turbo/
*.tsbuildinfo
# Pages deploy dir composed by scripts/build-site.mjs (site/ + apps/web build) — build artifact,
# not source
deploy/
# Cloudflare Workers (apps/relay) — local secrets and wrangler's local dev state
.dev.vars
.wrangler/
# design-sync machine state (staged scripts, build output, caches)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules/
